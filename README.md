# Game Menu Remove

A client-side Fabric mod for Minecraft that removes the "Report Bugs" and "Give Feedback" buttons from the pause menu. The buttons are filtered out by their translation keys (`menu.reportBugs` and `menu.sendFeedback`) before being added to the menu layout.